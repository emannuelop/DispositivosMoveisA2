import 'package:flutter/material.dart';

class SituacaoAcademicaScreen extends StatelessWidget {
  const SituacaoAcademicaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('SituacaoAcademica')),
      body: const Center(child: Text('Conteúdo da tela SituacaoAcademica')),
    );
  }
}
