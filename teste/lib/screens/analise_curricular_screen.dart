import 'package:flutter/material.dart';

class AnaliseCurricularScreen extends StatelessWidget {
  const AnaliseCurricularScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('AnaliseCurricular')),
      body: const Center(child: Text('Conteúdo da tela Analise Curricular')),
    );
  }
}
